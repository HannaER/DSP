#include <stdio.h>
#include "constants.h"
#include "database.h"


// input ska vara en lista med block_t structar i subset form
// gå igenom databasen och jämföra stoleken på error
void matching(block_t* input){
	int i;
	/*for(i = 0; i < SUBSET_LENGTH; i++){
		float temp_refl[N_REFLEC] = input[i]
		
	}*/
	
	
	return;	
}
